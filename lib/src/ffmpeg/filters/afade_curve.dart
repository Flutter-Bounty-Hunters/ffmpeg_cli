/// Options include
/// `tri`
/// `qsin`
/// `hsin`
/// `esin`
/// `log`
/// `ipar`
/// `qua`
/// `cub`
/// `squ`
/// `cbr`
/// `par`
/// `exp`
/// `iqsin`
/// `ihsin`
/// `dese`
/// `desi`
/// `losi`
/// `sinc`
/// `isinc`
/// `nofade`
enum AFadeCurve {
  tri,
  qsin,
  hsin,
  esin,
  log,
  ipar,
  qua,
  cub,
  squ,
  cbr,
  par,
  exp,
  iqsin,
  ihsin,
  dese,
  desi,
  losi,
  sinc,
  isinc,
  nofade
}
